# 英语奇遇记 · 项目交接文档

> 当前版本：**V1.0.0**（sw.js `CACHE_VERSION = 'ea-v1.0.0'`，2026-09-02，B 系列收官）
> 本文档给「下一个接手的会话/开发者」看：项目是什么、东西都在哪、规矩有哪些、从哪里继续。
> 逐批工作记录、验收结论、续跑说明在 `CHECKLIST.md`（本文档只给地图，不重复细节）。

## 一、项目是什么

给一个孩子（六年级，备考剑桥 KET）做的英语学习 PWA：纯静态站，无后端、无构建步骤，部署在 GitHub Pages（子目录，**所有路径必须相对路径**）。数据全存本机 localStorage，不上传。支持 1-9 年级 / KET / PET 三类模式（右上角年级选择器切换，模式驱动首页入口与 navigate 改道，文案随模式如实标注——不允许「入口叫 A 落地是 B」）。

## 二、目录地图

```
index.html            应用壳（单页，#app 容器 + 顶部/底部导航）
sw.js                 Service Worker：壳预缓存 + data/ 内容运行时缓存（cache-first）
                      ★ 改任何线上 JS 必须 bump CACHE_VERSION（全局单调递增，绝不复用）
assets/css/style.css  B1 设计系统 token 唯一事实源（:root）——与 tw-config.js 必须同步改
assets/js/
  app.js              路由 navigate()/答题态 enterFocus/弹窗/toast/首页/错题本/「我的」
  storage.js          localStorage 统一层（KEYS 枚举 + ea_ 前缀；导出/导入/重置只认 KEYS）
  tw-config.js        Tailwind class → token 映射（测量页也必须引用，否则渲染失真）
  utils/
    shuffle.js        四层随机抽题（题池抽样+错题加权+题序+选项洗牌 answer 同步重算）
    lazy-data.js      data/ 分片懒加载（并发合并+重试+内存缓存）
    ket-hall-map.js   KET 八课 ⇄ 语法大厅双向跳转映射
    exam-season.js    考季派生（不写死年份）
    ai-key.js         AI key/配置存取（独立裸键 eaAiKey/eaAiCfg，不进 KEYS——导出永无 key）
    ai-chat.js        AI 请求/降级/缓存/全部提示词（B6：错因/换说法/侦探判定/写作点评）
  modules/            年级模块（words/levels/grammar/reading/writing/recite/reinforce/pet…）
    grammar-hall/     语法大厅（hall 列表 + lesson 课内：九段讲解+四环节闯关+侦探关）
    exam/             KET 备考中心 11 模块（hub/plan/knowledge/grammar-course/reading-drill/
                      writing-lab/mock/checkin/resources/report + exam-common）
    ai-settings.js    AI 助手设置页（入口在「我的」）
data/
  grammar/g01~g50     语法大厅 50 课（3200 题+352 病句，题干全局无重复）＋早期 kindergarten/primary/junior
  exam/ket/           ★红线：grammar-lessons/errors/inventory 三文件「一个字不动」（守卫见下）
  exam/ket|pet/words  KET 1416 词 / PET 2143 词（词库 tips 已全站标点/引号规范化）
  words/ reading/ writing/ reader/  年级词库/阅读/写作/四阶读本（reader 数据层就位待内容）
tools/
  check-data.mjs      Schema + 编码守卫（mojibake 检测）——改任何中文数据后必跑
  check-esm.mjs       ESM 语法校验（★裸 node --check 验不了 ESM，出过线上事故）
  check-lesson.mjs    大厅课校验（跨 50 课题干查重 + 选项洗牌 answer 同步验证）
  check-ket-lessons-untouched.mjs  八课红线守卫（逐字段深比对+sha256，动手前先备份）
  backup/             各批次开工备份（B2~B5c…）
  smoke/              浏览器实跑自检：preflight.mjs（四项）/ verify-server.mjs（带断网开关）/
                      smoke.html（41+30 路由）/ sw-check / modal-check（0.9.33 答题态）/ shot（窄屏实测）
CHECKLIST.md          全部批次记录 + 环境坑清单 + 设计系统规范 + 版权红线 + B 系列总结
```

## 三、必须遵守的规矩（违反=返工）

1. **KET 八课数据红线**：`data/exam/ket/grammar-lessons.json` 等三文件一个字不动。动任何批次前 `cp` 到 `tools/backup/<批次>/`，收尾跑 untouched 守卫。
2. **中文文件一律 node `fs.writeFileSync` utf8**，禁 PowerShell 管道（会静默损坏编码且能过 Schema）；改完跑 `check-data.mjs` 编码守卫。
3. **改线上 JS → bump `sw.js` CACHE_VERSION**；新 JS 文件要登记进 sw 壳清单（preflight 会查）；`data/grammar/index.json` 变动也必须 bump（壳缓存 cache-first）。
4. **设计系统**：全站禁止硬编码字号/颜色/间距/圆角/阴影，只准引用 token（详见 CHECKLIST「设计系统规范」；豁免三类已登记）。
5. **返回与答题态**：新答题页必须接 `enterFocus` 且**不 bindBack**；`bindBack` 之后禁止再赋 `#examBackBtn.onclick`（双绑定竞态，B2 教训）。
6. **AI 六项安全红线**（B6a）：key 只存本机 localStorage 独立裸键；绝不进源码/sw 清单/console/错误信息；`getKey()` 只准在组装请求头处调用（现仅 ai-chat.js 与 ai-settings.js 测试连接两处）；UI 只显示尾号 4 位；每批收尾 grep 复核。
7. **AI 定位红线**（B6b）：AI 做增强不做替代——绝不现场生成语法讲解（50 课过了考纲边界验证）；所有 prompt 以本课/本题已有内容为硬上下文；不可用时优雅退回静态体验（无 key 不渲染按钮/离线即时提示/连败退避/失败指回考点解析）。
8. **版权红线**：PET/KET 内容 100% 原创；改编只限 1929 年前英文文学/美国联邦作品且 source 可追溯；剑桥官方材料绝不入库（.gitignore 已拦 Docs 下 pdf/zip/mp3）。
9. **健康护栏**：不做连续打卡/断签压力、空状态不催促、单日 120 分钟温和劝停、AI 不打断答题态不插话、写作 AI 不打分不代写。
10. **并行会话禁止同时改 sw.js**；每批「一类一 commit + 硬停机等家长验收」。

## 四、B 系列新增能力速查（细节见 CHECKLIST 对应批次）

- **语法大厅 50 课**（V0.9 P1-P4）：四层（基石/骨架/进阶/精修），每课九段讲解 + SVG 记忆卡 + 4 环节×16 题 + 侦探关；乐队比喻体系；与 KET 八课双向跳转。
- **学习闭环**（B4）：`QUIZ_MISTAKES` 错题落盘（12 字段口径**勿改**，是 AI prompt 的数据源）→ 错题本五分区 → 回课直达规则段 → 反向 chip → 答对自动毕业。
- **设计系统**（B1）：token 双事实源；窄屏验证用 `shot.html?page=x&w=360` 实测右边界（截图不可靠）。
- **AI 增强**（B6a/b/c）：设置页（我的→AI 助手设置，DeepSeek，服务商注册表可扩展）；四个点位——错题本&结果页错因解释（缓存 200 条）/讲解页换说法/侦探关判定（保守，孩子自评定夺）/写作点评（两处，不打分）；离线即时感知+联网自动复原+连败退避（会话级）。提示词全部集中在 `utils/ai-chat.js`，调质量只改这一个文件。

## 五、怎么跑验证（收尾标准流程）

```
node tools/smoke/preflight.mjs                    # 四项：ESM/Schema+编码/sw登记/ketLessonMap
node tools/check-ket-lessons-untouched.mjs tools/backup/<批次>
node tools/check-lesson.mjs data/grammar/g*.json  # 动过大厅课才需要
# 浏览器三件套（详见 CHECKLIST 环境坑 4/5/6/12/13：短路径 profile、测完杀进程、
# 真实时钟、全新 profile 防脏状态）：
node tools/smoke/verify-server.mjs tools/smoke/result.json 8100   # 后台
chrome --headless=new --user-data-dir=%TEMP%\eap http://127.0.0.1:8100/_smoke.html   # 41+30 路由
…/_swcheck.html  …/_modalcheck.html(?w=360)  …/_shot.html?page=x&w=360
# 行为级回归：历批临时检查页模式（b6c-check.html 形态，git 历史无存档，
# 写法参照 CHECKLIST 各批「行为级」条目，含 AI mock 网关与 onLine 覆盖技巧）
```

## 六、从哪里继续

**下一步入口 = CHECKLIST「🏁 B 系列总结」的遗留待办**：①浏览器后退 history 哨兵 ②闯练/模考进度快照 ③PET 词库补完到 3500 ④四阶读本 P5-P7（内容方案在家长的《V0.9-V1.0 扩容方案》总纲，**开工时请家长贴出，不要自创**）⑤AI 提示词按家长实测反馈调优。

每批开工前：读 CHECKLIST 末批记录与续跑说明 + ⚙环境坑清单；开工备份八课；收尾走第五节流程；bump 版本号；CHECKLIST 记录 + 写下批续跑说明；push 后**硬停机等家长验收**。
